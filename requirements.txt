# Speech recognition
SpeechRecognition==3.10.0
# PyAudio is platform-specific and may need manual installation
# For Windows: pip install pipwin && pipwin install pyaudio
# For Linux: sudo apt-get install python3-pyaudio
# For macOS: brew install portaudio && pip install pyaudio
webrtcvad==2.0.10

# Text-to-speech
pyttsx3==2.90
gTTS==2.3.2
playsound==1.3.0

# Natural language processing
transformers==4.30.2
torch==2.0.1

# API client
requests==2.31.0
aiohttp==3.8.5

# UI
# For Tkinter UI (current implementation)
# Tkinter comes with Python standard library

# Mock server
Flask==2.3.3

# Utilities
python-dotenv==1.0.0
numpy==1.24.3
sounddevice==0.4.6
soundfile==0.12.1

# Additional dependencies
wheel>=0.38.0
setuptools>=65.5.1